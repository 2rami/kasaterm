# Codex 프로젝트 지침

Claude가 이 프로젝트의 주 에이전트다. Codex는 작업을 시작하기 전에 다음 문서를 순서대로 읽고 따른다.

1. `CLAUDE.md` — 프로젝트 구조, 개발 규칙, 테스트 및 배포 절차의 기준 원본
2. `docs/agent-handoff.md` — Claude와 Codex가 공유하는 현재 상태, 결정, 남은 일

`CLAUDE.md`의 Claude 전용 명령이나 표현은 Codex의 대응 기능에 맞게 적용한다. 두 문서가 충돌하면 최신 공통 핸드오프를 우선하고, 프로젝트 규칙은 `CLAUDE.md`를 기준으로 한다.

## Codex 전용 규칙

- `.codex/config.toml`은 로컬 kasaspace 연결 설정이므로 사용자가 요청하지 않는 한 수정하거나 커밋하지 않는다.
- `.memory/MEMORY.md`가 있으면 참고하되, 파일이 없어도 작업을 계속한다.
- 여러 에이전트가 같은 워킹트리를 사용할 수 있으므로 기존 변경과 다른 에이전트가 시작한 프로세스를 건드리지 않는다.
- 이모지를 코드, 주석, 문서, 커밋 메시지, 앱 출력에 추가하지 않는다. 아이콘은 SVG를 사용한다.
- 주석은 코드만으로 드러나지 않는 이유, 배경, 제약만 설명한다.
