# Product

<!-- impeccable:product-schema 1 -->

## Platform

adaptive

## Users

AI 에이전트와 함께 일하는 1인 개발자와 개발 입문자가 중심 사용자다. 여러 터미널과 에이전트의 상태를 한곳에서 파악하고, 복잡한 설정 없이 바로 작업을 시작하려는 사람을 위한 제품이다.

## Product Purpose

kasaterm은 터미널을 코어로 둔 네이티브 데스크톱 작업 OS다. 터미널, workspace, AI 에이전트 오케스트레이션을 하나의 흐름으로 연결해 사용자가 첫 설치 직후부터 Claude Code나 Codex와 실제 개발 작업을 시작할 수 있게 한다.

첫 설치의 성공은 다음 설정을 마치거나 건너뛴 뒤, 바로 첫 터미널을 사용할 수 있는 상태다.

- 테마와 외형 선택
- 기존 Claude Code 또는 Codex 로그인 감지 및 기본 사용, 미로그인 상태라면 로그인
- Windows에서 사용할 셸 선택
- macOS에서 Apple Terminal 또는 iTerm2의 색상·폰트 가져오기, 또는 직접 설정

숙련 사용자는 온보딩을 건너뛸 수 있어야 하며, 기존 사용자에게 첫 설치 온보딩을 다시 노출하지 않는다.

## Positioning

kasaterm은 터미널에 AI 기능을 덧붙이는 도구가 아니라, 세 층이 함께 작동하는 하나의 작업 OS다.

1. **터미널** — GPU 셀 렌더링, PTY, 입력, multipane을 담당한다.
2. **workspace** — 파일, Git, pane, 에이전트 연결을 담당한다.
3. **오케스트레이션** — 여러 AI 에이전트의 작업과 협업 상태를 한눈에 다룬다.

세 층은 하나의 제품 안에서 함께 진화하며, 사용자는 터미널 명령과 에이전트 작업을 분리된 도구로 오가지 않는다.

## Operating Context

- macOS와 Windows의 네이티브 데스크톱 환경에서 사용한다.
- macOS와 Windows의 셸, 터미널 설정, 단축키, 시스템 관습에 맞게 경험을 조정한다.
- Claude Code와 Codex의 기존 인증 상태가 있으면 새 로그인을 강요하지 않고 이를 기본으로 사용한다.
- 로그인 상태 감지 구현은 Orca 저장소의 기존 동작을 참고한다.
- 초보자는 단계별 온보딩을 따르고, 숙련자는 건너뛰어 곧바로 터미널로 진입한다.

## Capabilities and Constraints

- 제품명과 패키지·바이너리명은 `kasaterm`이다.
- 온보딩은 첫 설치에서만 표시하고, 완료 또는 건너뛰기 상태를 기억한다.
- macOS에서는 Apple Terminal과 iTerm2의 색상·폰트를 가져오는 경로와 kasaterm에서 직접 설정하는 경로를 모두 제공한다.
- Windows에서는 사용자가 사용할 셸을 선택할 수 있어야 한다.
- Claude Code 또는 Codex의 기존 로그인 상태를 우선 감지하고, 감지되지 않을 때만 로그인을 안내한다.
- 공개 저장소이므로 키, 토큰, 인증정보를 저장소·설정·로그에 저장하지 않는다. 인증은 각 제공자의 기존 인증 상태를 사용하며 kasaterm이 비밀값을 복제하거나 보관하지 않는다.

## Brand Commitments

- 제품명은 소문자 `kasaterm`이다.
- 제품 정체성은 “AI 에이전트와 함께 일하는 1인 개발자를 위한, 터미널을 코어로 둔 작업 OS”다.
- 터미널, workspace, 오케스트레이션의 세 층은 분리된 제품이 아니라 하나의 경험으로 유지한다.

## Evidence on Hand

- `CLAUDE.md` — 세 층 구조, 핵심 사용자, 제품 정체성, 공개 저장소의 보안 제약
- `README.md` — 터미널 엔진, workspace, AI 에이전트 오케스트레이션의 현재 기능과 플랫폼 근거
- Orca 저장소 `src/main/claude-accounts/service.ts` — Claude Code는 사람용 문구가 아니라 `claude auth status --json`을 읽고, Codex는 기존 인증 저장소의 유무를 비밀값 노출 없이 판정하는 구현 참고자료다.
- 사용자 연구, 사용성 지표, 고객 사례는 아직 제공되지 않았다. 이후 제품 작업에서 이를 만들어내거나 암시하지 않는다.

## Product Principles

1. **첫 터미널까지 짧게** — 첫 설치 설정은 실제 작업 시작에 필요한 것만 다룬다.
2. **이미 있는 환경을 존중** — 기존 로그인, 셸, 색상, 폰트를 감지하고 재사용한다.
3. **초보자에게는 안내, 숙련자에게는 탈출구** — 단계별 설명과 건너뛰기를 함께 제공한다.
4. **세 층을 하나의 흐름으로** — 터미널, workspace, 오케스트레이션 사이의 불필요한 왕복을 만들지 않는다.
5. **인증정보는 kasaterm의 자산이 아니다** — 비밀값을 복제하거나 보관하지 않는다.
